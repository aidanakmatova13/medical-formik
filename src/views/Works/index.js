import React from 'react';
import Layout from "../../components/Layout";

const Works = () => {
    return (
        <Layout>
            tasks and works
        </Layout>
    );
};

export default Works;