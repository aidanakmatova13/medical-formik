import React from 'react';
import Layout from "../../components/Layout";

const Calendar = () => {
    return (
        <Layout>
            Calendar
        </Layout>
    );
};

export default Calendar;